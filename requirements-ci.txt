# Complete synthetic/unit/UI CI environment; no hardware SDKs.
# Editable members are resolved together, never fetched by name from an index.
-e ./libs/python/capture_protocol
-e ./libs/python/capture_session
-e ./libs/python/capture_analysis[analysis]
-e ./libs/python/capture_worker
-e ./desktop[analysis]
grpcio-tools>=1.62,<1.63
mcap>=1.1,<2
pytest>=8
pytest-cov>=5
ruff>=0.6,<1
